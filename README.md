# Portfolio Example — GSAP Animation Lab 🎬

این پروژه ابتدا برای پیاده‌سازی یک پورتفولیو شخصی شروع شد، اما به‌مرور به یک فضای تمرین برای ساخت **انیمیشن‌های پیشرفته وب با GSAP** تبدیل شد. تمرکز اصلی این مخزن روی الگوهای تعاملی، افکت‌های حرکتی و تکنیک‌های بهینه برای اجرای انیمیشن‌های نرم و روان در محیط React/Next.js است.

<!-- نیاز به ویدیو یا GIF کوتاه از تعاملات کلیدی (منو، پارالاکس، نشانگر سفارشی) -->

## ویژگی‌های برجسته انیمیشن‌ها

- **منوی تمام‌صفحه با Flip**: باز و بسته شدن منو با استفاده از GSAP Flip و مدیریت حالت‌ها، شامل:
  - تبدیل آیکن همبرگری به ضربدر و برعکس با چرخش خطوط (`.line-1`, `.line-2`).
  - **Staggered reveal** برای آیتم‌های منو با ease مناسب.
  - **حفظ حالت هندسی** کانتینر با Flip State برای انیمیشن مرزها و ابعاد.  
  <!-- اسکرین‌شات از منوی باز/بسته با خطوط در حال چرخش -->

- **نشانگر سفارشی (Custom Cursor)**: نقطه‌ای پویا که به موس می‌چسبد و بر اساس زمینه تغییر شکل می‌دهد:
  - دنبال‌کردن موس با اینرسی و ease نرم.
  - **بزرگ‌نمایی** هنگام ورود روی عناصر تعاملی مانند دکمه‌ها و لینک‌ها.
  - **حالت متن**: باریک و کشیده شدن هنگام عبور روی نوشته‌ها برای افزایش حس تمرکز.
  - **اثر مگنتی** روی عناصر دارای `data-magnetic` با نزدیک شدن به مرکز آن عنصر.
  - **افکت کلیک**: burst/ripple کوچک هنگام کلیک.  
  <!-- GIF کوتاه از رفتار نشانگر روی لینک و متن و حالت مغناطیسی -->

- **پارالاکس ماوسی واکنش‌گرا**: حرکت لایه‌ها بر اساس موقعیت موس با کنترل شدت جداگانه برای هر آیتم:
  - تعیین شدت با `data-strength` برای هر المان دارای `data-parallax-item`.
  - **Media Query آگاه**: در دسکتاپ از حرکت موس استفاده می‌شود؛ در موبایل انیمیشن‌های آهسته‌ی yoyo با `sine.inOut` اجرا می‌گردد.
  - بازگشت الاستیک عناصر به حالت اولیه در خروج موس از محدوده.  
  <!-- اسکرین‌شات/ویدیو از پارالاکس لایه‌ها در دسکتاپ و شناوری آرام در موبایل -->

- **Marquee و Image Blur**:
  - نوار متن/لوگو با حرکت پیوسته (marquee) و کنترل سرعت/توقف هنگام hover.
  - کامپوننت **ImageBlur** برای نمایش تدریجی تصاویر با بلور و fade-in.  


### نمای کلی از انیمیشن های طراحی شده:


https://github.com/user-attachments/assets/6eca77b3-e46d-47f0-ae5c-82865c1cae53


## ساختار کد (خلاصه)
```
src/
├─ app/                      # Next.js App Router
├─ components/
│  ├─ Menu.tsx               # منوی تمام‌صفحه با GSAP Flip و تایم‌لاین‌ها
│  ├─ CustomCursor.tsx       # نشانگر سفارشی با Observer، hover states و اثر مگنتی
│  ├─ MouseParallax.tsx      # پارالاکس موس با matchMedia و تنظیم شدت آیتم‌ها
│  ├─ Marquee.tsx            # حرکت پیوسته متن/لوگو (marquee)
│  └─ ImageBlur.tsx          # نمایش تدریجی تصویر با افکت Blur/Fade
└─ utils/                    # ابزارها و کمک‌کننده‌ها (در صورت نیاز)
```

## جزئیات فنی مهم

- **GSAP Core + Plugins**:
  - استفاده از `@gsap/react` و هوک `useGSAP` برای کنترل lifecycle در React.
  - بهره‌گیری از `Flip` برای انیمیشن‌های stateful و جابه‌جایی layout بدون پرش.
  - استفاده از `Observer` برای دریافت رویدادهای pointer و ایجاد افکت‌های پویا.

- **الگوهای تایم‌لاین**:
  - ساخت تایم‌لاین‌های چندمرحله‌ای برای هماهنگی انیمیشن‌ها (آیکن، آیتم‌ها، فوتر منو).
  - استفاده از `stagger`, `ease`, `delay` برای توالی روان.

- **بهینه‌سازی و UX**:
  - guard کردن **isAnimating** برای جلوگیری از کلیک‌های دوبل هنگام باز/بسته‌سازی منو.
  - `matchMedia` برای تطبیق رفتار انیمیشن در دسکتاپ/موبایل.
  - cleanup کامل event listenerها برای جلوگیری از memory leak.

## راه‌اندازی

### پیش‌نیازها
- Node.js 18+
- pnpm یا npm

### نصب
```bash
pnpm install
# یا
npm install
```

### اجرا (Dev)
```bash
pnpm dev
# یا
npm run dev
```

## نکات تکمیلی و مسیر توسعه
- برخی اجزای UI صرفاً برای **نمایش الگوهای انیمیشن** ساخته شده‌اند و به داده‌های واقعی متصل نیستند.
- می‌توان سرعت، ease و شدت افکت‌ها را از طریق props یا data-attributes تنظیم‌پذیر کرد.


